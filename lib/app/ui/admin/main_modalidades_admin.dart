import 'package:flutter/material.dart';

class MainModalidadesAdmin extends StatelessWidget {
  const MainModalidadesAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
