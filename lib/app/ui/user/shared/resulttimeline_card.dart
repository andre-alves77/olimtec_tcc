import 'package:flutter/material.dart';

class ResultTimelineCard extends StatelessWidget {
  const ResultTimelineCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}